{
  "name": "luxury-todo-server",
  "version": "v1.0.0-19",
  "description": "Local server for Luxury Todo app with hot reload and HTTPS",
  "scripts": {
    "start": "npm install && serve -l 8080 -s .",
    "dev": "npm install && serve -l 8080 -s .",
    "setup": "npm install",
    "version": "node scripts/update-version.js"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "keywords": [
    "server",
    "todo",
    "pwa",
    "mobile",
    "localhost",
    "hot-reload"
  ],
  "author": "Raúl Correia",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
